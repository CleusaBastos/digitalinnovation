package one.digitalinnovation.testes

import one digitalinnovation.dig1onebank.clienteTipo

fun main(){
    clienteTipo.values().forEach { elemento 
        printin("&{elemento.name) * $elemento")
    }
    clienteTipo.values().forEach { elemento 
        printin("${it.name} * $it)
    }   